[project]
name = "lab14"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pyswip>=0.2.10"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
